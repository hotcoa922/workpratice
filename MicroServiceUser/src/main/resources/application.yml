server:
  port: 5001

spring:
  application:
    name: MicroServiceUser
  datasource:
    url: jdbc:mariadb://localhost:3306/userservice
    username: admin123
    password: admin123
    driver-class-name: org.mariadb.jdbc.Driver

mybatis:  # src/main/resources/mappers/ 하위
  mapper-locations: classpath:/mappers/**/*.xml

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka